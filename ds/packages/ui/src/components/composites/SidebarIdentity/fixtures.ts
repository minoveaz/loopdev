import { SidebarIdentityProps } from './types';
import React from 'react';

export const SIDEBAR_IDENTITY_FIXTURES: Record<string, any> = {
  marketing: {
    name: 'Marketing Studio',
    accentColor: 'var(--lpd-color-brand-primary)',
    isRail: false
  },
  rail: {
    name: 'Marketing Studio',
    accentColor: 'var(--lpd-color-brand-primary)',
    isRail: true
  }
};
