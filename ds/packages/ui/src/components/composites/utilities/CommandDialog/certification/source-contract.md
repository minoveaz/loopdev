# Source contract: CommandDialog

- Implementation: `index.tsx`
- Public data and copy: required through `CommandDialogProps`.
- Fixture ownership: `fixture.tsx` and consumer showcase fixtures only.
- Forbidden implementation content: domain data, default visible copy, raw
  palette values, literal z-indexes and inline visual styles.
- Global evidence: `pnpm certification:source-contracts`.