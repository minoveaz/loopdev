import { ThemeToggleProps } from './types';

export const THEME_TOGGLE_FIXTURES: Record<string, ThemeToggleProps> = {
  header: {
    variant: 'technical',
    size: 'md',
  },
  minimal: {
    variant: 'standard',
    size: 'sm',
  }
};
