import { BlueprintBackgroundProps } from './types';

export const BLUEPRINT_BACKGROUND_FIXTURES: Record<string, BlueprintBackgroundProps> = {
  launchpad: {
    intensity: 'low',
    withScanline: true,
    variant: 'blueprint'
  },
  ai_context: {
    intensity: 'medium',
    withScanline: true,
    variant: 'neural'
  }
};
