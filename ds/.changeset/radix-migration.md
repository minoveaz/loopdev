---
"@loopdev/ui": minor
---

Refactor: Migration to Radix UI + shadcn/ui architecture.
- Replaced basic HTML button with Radix Slot for polymorphism.
- Integrated `class-variance-authority` for type-safe variants.
- Fixed `cn` utility to use `tailwind-merge` properly.
