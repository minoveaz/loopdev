{
  "name": "my-company-web-platform",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rimraf node_modules .turbo"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  }
}
