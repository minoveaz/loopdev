# Archived Platform Shell Blueprint

`UI_SHELL_BLUEPRINT.md` is retained as historical context only. It does not
define the current shell composition.

The current direction is `SuiteShell` + `SuiteCanvas`, governed by the active
platform-shell contracts and implementation evidence.

Archived on 2026-08-13 after user confirmation.
