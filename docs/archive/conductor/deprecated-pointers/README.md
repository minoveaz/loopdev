# Archived deprecated pointers

These files were compatibility pointers under `conductor/`. They contain no
current LoopDev guidance and are retained only as historical evidence of the
transition to the governed documentation structure.
