#!/bin/sh

branch=$(git branch --show-current)
case "$branch" in
	develop|main)
		echo "Direct pushes to $branch are not allowed. Open a Pull Request instead."
		exit 1
		;;
esac

pnpm exec node scripts/validate-git-conventions.mjs
git diff --check